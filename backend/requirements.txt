fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.12.5
pydantic-settings==2.13.1
# NOTE: newer supabase-py currently pulls in pyiceberg on Windows/Python 3.14.
# The backend includes a tiny local shim for that optional analytics import so
# the client can use modern sb_secret keys without requiring native build tools.
supabase==2.28.3
google-genai>=1.0.0
httpx==0.27.0
# h2 is intentionally excluded from explicit requirements, but may arrive
# transitively from newer supabase-py versions.
python-jose[cryptography]==3.3.0
google-auth==2.30.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.133.0
yt-dlp==2024.5.27
ffmpeg-python==0.2.0
arq==0.25.0
sentry-sdk[fastapi]==2.6.0
slowapi>=0.1.9
psycopg2-binary==2.9.9
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
